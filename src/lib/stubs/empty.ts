// Stub for @mediapipe/* packages imported by bundled ESM code but never
// used at runtime (we use runtime: 'tfjs', not 'mediapipe').
// Named exports satisfy static import analysis; they are never instantiated.
export default {};
export class FaceMesh {}
export class SelfieSegmentation {}
