---
import { slugifyTag } from '../lib/slugify';

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

{tags.length > 0 && (
  <ul class="flex flex-wrap gap-2 list-none m-0 p-0">
    {tags.map((tag) => (
      <li>
        <a
          href={`/tags/${slugifyTag(tag)}`}
          class="inline-flex items-center font-mono text-[10px] uppercase tracking-widest px-2 py-0.5 border border-border text-ink-faint rounded hover:border-terracotta hover:text-terracotta transition-colors no-underline"
        >
          {tag}
        </a>
      </li>
    ))}
  </ul>
)}
