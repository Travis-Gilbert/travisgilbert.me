'use client';

import ExplorerShell from '@/components/theseus/explorer/ExplorerShell';

/** Cosmograph surface for the Theseus graph. */
export default function ExplorerPanel() {
  return <ExplorerShell />;
}
