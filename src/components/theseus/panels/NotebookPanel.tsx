'use client';

import NotebookLayout from '@/components/theseus/notebook/NotebookLayout';

/**
 * NotebookPanel: wrapper for PanelManager.
 * Renders the three-panel notebook layout (doc list, editor, workbench).
 */
export default function NotebookPanel() {
  return <NotebookLayout />;
}
