# Local/experimental requirements (Travis's machine only)
# Production uses requirements/base.txt + production.txt (no PyTorch)
#
# Install with:
#   pip install -r requirements/local.txt --break-system-packages

--extra-index-url https://download.pytorch.org/whl/cpu

-r base.txt

# Full local NLP stack
torch>=2.0
sentence-transformers>=3.0
faiss-cpu>=1.7

# Graph algorithms (needed for Batch 2: PageRank, paths)
networkx>=3.2
